# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Infusoria' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Infusoria

  target 'InfusoriaTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Widget' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Widget
  pod 'Alamofire', '~> 4.9.0'
  pod 'FeedKit', '~> 9.0'
end
